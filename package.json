{
  "name": "agent-relay",
  "version": "1.1.0",
  "description": "Authenticated, resilient WebSocket relay for multi-agent AI communication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check agent.js && node --check websocket.js"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
